#include "ethernet.h"
#include "esp_log.h"

static const char *TAG = "NETWORK";

esp_err_t network_ethernet_init(void) {
    ESP_LOGI(TAG, "ethernet: not implemented (Phase 2)");
    return ESP_OK;
}
