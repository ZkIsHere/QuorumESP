#include "watchdog.h"
#include "esp_log.h"

static const char *TAG = "WATCHDOG";

esp_err_t quorumesp_watchdog_init(void) {
    ESP_LOGI(TAG, "watchdog: not implemented (Phase 2)");
    return ESP_OK;
}
