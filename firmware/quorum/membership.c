#include "membership.h"
#include "esp_log.h"

static const char *TAG = "QDEVICE";

esp_err_t quorum_membership_init(void) {
    ESP_LOGI(TAG, "membership: not implemented (Phase 3)");
    return ESP_OK;
}
